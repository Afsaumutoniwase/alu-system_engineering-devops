#!/usr/bin/env bash
ln -sf /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default
service nginx start
kill "$(pgrep 'nginx' | head -1)"
service nginx status | grep -q "active (running)" && echo "1" || echo "* nginx is not running"
